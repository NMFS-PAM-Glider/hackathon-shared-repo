# Python packages used by this repo.
#
#   pip install -r requirements.txt

jupyterlab
pandas
requests
matplotlib

# For notebook 04: ERDDAP access and gridded satellite data.
erddapy
xarray
netcdf4
pydap

# For notebook 06: STAC catalogue client.
pystac-client

# For notebook 07: noise processing. These are what NoiseApp (YAWN) needs.
# NoiseApp itself is not on PyPI; a copy lives in noiseprocessing/ in this repo.
h5py
scipy
seaborn
soundfile
google-cloud-storage

# For notebook 08: comparing NoiseApp against PyPAM. Installs as
# lifewatch-pypam but imports as `pypam`.
lifewatch-pypam
pyhydrophone
